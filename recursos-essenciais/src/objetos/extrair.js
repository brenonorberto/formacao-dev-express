const carrinho = {
  qtdeItens: 3,
  total: 13562.12,
  itens: [{}, {}, {}],
};

const { qtdeItens, total } = carrinho;

console.log(qtdeItens);
console.log(qtdeItens);
console.log(total);
console.log(qtdeItens);
console.log(total);

const nomeCompleto = ["Pedro", "Silva", "Pereira"];

const [primeiroNome, , ultimoNome] = nomeCompleto;
console.log(primeiroNome, ultimoNome);
